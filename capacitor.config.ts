import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.Nebre.budget-management-app.',
  appName: 'budget-management-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
