import { ObjectBasePrototype } from './object-base-prototype';

describe('ObjectBasePrototype', () => {
  it('should create an instance', () => {
    expect(new ObjectBasePrototype()).toBeTruthy();
  });
});
